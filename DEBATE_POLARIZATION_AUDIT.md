# 多 Agent 辩论的极化审计 + 离线确定性实验

> **先划清这次测了什么、没测什么。**
> 实验用**脚本化桩模型**驱动**真实的图**（`web.runner._run` → 真实 14 阶段流水线），
> 全部离线、可重复、进 CI。它测的是**架构**：一条说法在流水线里怎么传播、会不会被丢弃、
> 会不会改变终局评级。它**不**测 DeepSeek 有多大倾向去极化——那是模型的行为分布，
> 桩模型给不出这个答案。要看真实模型请用 `marvel/harness/` 的录制能力跑一次真调用。

审计与实验代码：`tests/test_debate_polarization.py`（12 例，`python -m pytest` 可复现）

---

## 1. 结构审计：传播链长什么样

按代码（不是按文档）逐段核对：

| 阶段 | 它看到什么 | 它看不到什么 |
|---|---|---|
| 9 个分析师 | 各自的工具与数据 | 彼此的报告 |
| **Bull / Bear 辩手** | **9 份分析师报告全文**（逐字嵌入提示词）+ 质量门控摘要 + 完整辩论历史 + 对方上一轮发言 | — |
| **Research Manager（裁判）** | **只有辩论记录**（`history`） | **9 份原始报告一份都没有** |
| Trader | 投资计划 | 原始报告 |
| 三方风险辩论 | 交易计划 + 彼此的历史与上一轮发言 | 原始报告 |
| Portfolio Manager | 风险辩论记录 | 原始报告 |

代码证据：

- `researchers/bull_researcher.py:41-53` —— `Market research report: {market_research_report}` …
  `Latest news report: {news_report}` … `Last bear argument: {current_response}`；
  `bear_researcher.py` 对称，看 `Last bull argument`。
- `managers/research_manager.py:42` —— 提示词只由 `{history}` 拼成，然后要求它按
  Buy/Overweight/Hold/Underweight/Sell 五档给评级。
- `managers/portfolio_manager.py:50` —— 「synthesize the risk analysts' debate」，
  输入同样是辩论记录。
- `risk_mgmt/*.py` —— 三方共享 `risk_debate_state` 的 `history` 与各自的
  `current_*_response`。

由此得到三条**结构性**结论（不需要实验就能读出来，实验只是把它们钉死）：

1. **没有一道工序的职责是「核实来源」。** 没有任何 agent 被要求判断一条说法是否可信，
   也没有任何字段用来记录「未经证实」。
2. **裁判看不到证据。** 经理只在辩论文本上做判断——它无法回到原始报告复核一句话是不是
   被辩手放大了。这不是实现疏漏，是这一层的输入就长这样。
3. **多空辩手是唯一的传播通道，也是唯一的防火墙。** 报告原样进辩论，但经理只看辩论；
   于是「辩手有没有重复这条说法」直接决定了它能不能到达评级。

另外两条会放大结论的机制：

- **质量门控不查来源。** 硬检查看的是长度与表格结构，LLM 复审看的是报告质量；
  一条格式完好、内含捏造消息的报告**会被判 A**（下面的实验直接测了这一点）。
- **记忆日志跨轮回注**（`memory.py` → `past_context`）。一次基于假消息得出的结论会被
  写成「过往教训」，并注入同一标的之后的运行——错误结论可以长期存活。
  （第 2 项工作已给它加了 `as_of` 日期上界，所以它至少不会跨时间点泄漏。）

---

## 2. 实验设计

一条哨兵假消息注入 `news_report`：

```
【独家爆料】据三名知情人士透露，公司已被证监会立案调查财务造假，虚增收入约120亿元
```

桩模型在**每个阶段**应用同一条规则：「如果我的提示词里出现这条说法，我就
①忽略它 / ②重复并升级它 / ③引用它但明确驳斥它」。三种处置 × {注入, 不注入} = 6 次运行，
除这一处外完全一致，所以差异只能来自注入的那条说法。

评级映射到序数：Buy=2, Overweight=1, Hold=0, Underweight=-1, Sell=-2。

---

## 3. 实测结果

（`python -m pytest tests/test_debate_polarization.py` 可复现；下表由同一套代码跑出）

| 辩手处置 | 注入假消息 | 终局评级 | 序数 | 携带该说法的产物 |
|---|---|---|---|---|
| 忽略 | 否 | Hold | 0 | — |
| 忽略 | **是** | Hold | 0 | 1 个（仅 `news_report`） |
| 重复升级 | 否 | Hold | 0 | — |
| 重复升级 | **是** | **Sell** | **−2** | **16 个**（含 `final_trade_decision`） |
| 引用但驳斥 | 否 | Hold | 0 | — |
| 引用但驳斥 | **是** | Hold | 0 | **16 个**（含 `final_trade_decision`） |

空白对照干净（不注入时评级与携带数都不动），所以实验本身成立。

### 读数

1. **位移 2 档。** 辩手照单全收时，一条来自单一来源、未经核实的说法把终局评级从
   Hold 推到 Sell——而这条说法从未出现在任何裁决阶段的输入里，它是**通过辩手的复述**
   抵达终局的。
2. **放大 16 倍。** 注入 1 处 → 16 个产物携带它，覆盖投资辩论、投资计划、三方风险辩论、
   直到最终交易决策。没有任何环节把它删掉。
3. **「驳斥」不等于「剔除」，这条最值得注意。** 第三种处置下评级没有动（Hold），
   但该说法**依然出现在最终交易决策里**。也就是说：结论被保护了，**报告本身没有**——
   最终交付物仍然可以引用一条捏造的爆料。对一份要给人看的研究报告来说，
   这与结论被带偏是两类不同的风险，而现有流水线只防住了前者的「侥幸」情形。
4. **唯一真正的防火墙是「没人重复它」。** 「忽略」那一行里，说法停在 `news_report`，
   经理的提示词里根本没有它——因为经理只看辩论记录。这是架构里唯一确定性的防线，
   而它依赖的是辩手的行为，不是任何校验步骤。
5. **门控不是防线。** 同一份含假消息、格式完好的报告，硬检查判 **A**
   （`test_a_well_formed_report_with_a_false_claim_is_graded_A`）。

---

## 4. 这些结论的性质

| 结论 | 性质 |
|---|---|
| 没有来源核实工序 / 裁判看不到证据 / 辩手是唯一通道 | **架构性**，读代码即可确认，与模型无关 |
| 门控给带假消息的报告判 A | **架构性**，已实测 |
| 说法传播到 16 个产物 | **架构性**（只要辩手复述就会发生） |
| 位移 2 档、驳斥能保住结论 | **取决于模型行为**，本次由桩模型设定，不代表 DeepSeek |

第 4 行是这次实验最容易被误读的地方，所以单列出来。

---

## 5. 如果要把结论变硬，下一步可以做什么

按「性价比 / 是否需要你拍板」排序，**均未实施**：

1. **给假消息一个可判定的标记位**（成本低、收益直接）。让新闻工具对「单一来源 /
   无法交叉验证」的条目在输出里打标，并把「未经证实」作为质量门控的一个硬检查项——
   门控已经是唯一带复核职责的工序，让它查来源比新增一个 agent 便宜得多。
2. **让裁判看到证据**。给经理的提示词里附上被引用报告的关键字段（或至少让它可以要求
   回查），否则「辩论文本 → 评级」这一步永远无法证伪辩手的放大。
3. **最终交付物里剔除未证实的说法**。第 3 条读数说明：即使结论没被带偏，报告里仍可能
   印着捏造消息。可以在 PM 之后加一道「引用来源核对」，或在报告生成时剥掉带未证实标记的
   引文。
4. **用真实模型复跑本实验**。`marvel/harness/` 的录制能力可以让一次真实运行被回放，
   于是「换一个处置假设」不需要再花一次 token。需要你给预算上限。

需要你选哪几条我再动手——这几条都会改变 agent 的输入或输出契约，
不适合由我替你定。
